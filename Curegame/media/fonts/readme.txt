NOTE: Rename the TTF/OTF font to the font name, i.e. "erasedst.ttf" -> "EraserDust.ttf". 

We have to do this to correctly register the font in registry since H3D (FreeType) uses a raw regsitry lookup to find the font.

